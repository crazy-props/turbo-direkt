import React from 'react';
import AppFooterGrid from "./AppFooterGrid"

function AppFooter() {
    return (
        <div>
            <AppFooterGrid />
        </div>
    );
}
export default (AppFooter);